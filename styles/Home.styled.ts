import styled from 'styled-components';

export const ColumnContainer = styled.div`
    display: flex;
    flex-direction: row;
    min-height: 400px;
    margin-top: 2rem;
    overflow-x: auto;
    overflow-y: visible;
`;